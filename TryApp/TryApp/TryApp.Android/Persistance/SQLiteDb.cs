using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

using Android.App;
using Android.Content;
using Android.OS;
using Android.Runtime;
using Android.Views;
using Android.Widget;
using SQLite;
using TryApp.Persistance;

//[assembly:Dependency(typeof(SQLiteDb))]
namespace TryApp.Droid.Persistance
{
    public class SQLiteDb// : ISQLiteDb
    {
     //  public SQLiteAsyncConnection GetConnection()
     // {
     //       var documentsPath = Environment.GetFolderPath(Environment.SpecialFolder.);
     //        throw new SQLiteConnection(path);
     // }
    }
}